server:
  port: 10023

spring:
  datasource:                              # 本服务的业务库（与其他服务一致，走 tj-mysql 容器）
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${TJ_MYSQL_HOST:127.0.0.1}:${TJ_MYSQL_PORT:3306}/tj_promotion?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: ${TJ_MYSQL_USERNAME:root}
    password: ${TJ_MYSQL_PASSWORD:root}
  grpc:
    # 本服务 gRPC 服务端端口（约定 = HTTP 端口 + 1000，可被 TJ_GRPC_<服务>_PORT 覆盖）
    server:
      port: ${TJ_GRPC_PROMOTION_PORT:11023}

  cloud:
    stream:
      bindings:
        couponReceive-out-0: { destination: promotion.topic, producer: { routing-key-expression: "'coupon.receive'" } }

powerjob:
  worker:
    enabled: true
    app-name: promotion-service
    server-address: ${TJ_POWERJOB_HOST:127.0.0.1}:${TJ_POWERJOB_PORT:7700}
    allow-lazy-connect-server: true   # Server 未就绪也能启动（老项目 xxl-job 同样容忍）
tj:
  swagger:
    package-path: com.tjxt.tjpromotion           # 本服务 Controller 所在包（文档按包分组扫描）
